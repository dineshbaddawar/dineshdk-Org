public class AsyncExecutionJobChaining implements Queueable {

    public void execute(QueueableContext Context){
        Account acc=new Account(Name='Facebook',phone='980988-890');
        insert acc;
    }
}